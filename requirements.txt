chainlit>=1.3.0
langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── LLM providers — install only what you use ─────────────────────────────────
langchain-openai>=0.3.0          # OpenAI + any OpenAI-compatible endpoint (required)
# langchain-anthropic>=0.3.0    # Anthropic Claude  (uncomment to enable)
# langchain-google-genai>=2.0.0 # Google Gemini     (uncomment to enable)

# ── Confluence RAG (confluence_qa workflow) ────────────────────────────────────
chromadb>=0.5.0                  # Vector store for Confluence knowledge base
langchain-chroma>=0.1.0          # LangChain Chroma integration
beautifulsoup4>=4.12.0           # HTML parsing for Confluence page content
# atlassian-python-api>=3.41.0  # Confluence REST API client (uncomment if using live Confluence)
# langchain-huggingface>=0.1.0  # Local embeddings via sentence-transformers (uncomment for EMBEDDING_PROVIDER=local)
# sentence-transformers>=3.0.0  # Required by langchain-huggingface for local embeddings
