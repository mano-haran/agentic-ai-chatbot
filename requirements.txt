chainlit>=1.3.0
langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── LLM providers — install only what you use ─────────────────────────────────
langchain-openai>=0.3.0          # OpenAI + any OpenAI-compatible endpoint (required)
# langchain-anthropic>=0.3.0    # Anthropic Claude  (uncomment to enable)
# langchain-google-genai>=2.0.0 # Google Gemini     (uncomment to enable)
